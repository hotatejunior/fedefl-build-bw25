[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q
# Integration tests that need the built brightway project / large data assets
# skip themselves when those are absent (see tests/test_*.py), so the unit
# tests always run — including in CI where only the Python env is available.
